pipeline {
    agent {
        docker { image 'node:apline'}
    }
        stages
        {
            stage('Four') {
                steps {
                    sh 'node --version'
                }
            }
        }
}